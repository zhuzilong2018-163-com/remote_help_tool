HOME=/root/my_work1/remote_help_tool

COMMON=$(HOME)/common
SERVER=$(HOME)/server
CLIENT=$(HOME)/client
OUTPUT=$(HOME)/target

COMM_INCLUDE=$(COMMON)/inc

CC=gcc
STRIP=strip
AR=ar
CXX=g++

RELEASE=$(OUTPUT)/release
RELEASE_LIB=$(OUTPUT)/lib
RELEASE_BIN=$(OUTPUT)/bin